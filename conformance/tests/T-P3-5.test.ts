/**
 * T-P3-5 — P-3 (Public-data replay).
 *
 * Classes: VERIFIER.
 * Register: NAMED (§11.2)
 *
 * §A's sketch, verbatim — the scope of this test, which is not widened without
 * a decision (`conformance/README.md`):
 *
 *   A fixture whose settlement, `connection_created`, and resolution manifest precede the window's `from`, with chunks inside it, appraises verified and its bundle carries all three.
 *
 * NOT EXPANDED. The body below fails on purpose. A test that is not written
 * must not report that it passed, and must say what it is for while it waits.
 */
import { test } from 'node:test';

test('T-P3-5 — Public-data replay', () => {
  throw new Error(
    'T-P3-5 NOT EXPANDED — serves P-3 (Public-data replay), classes VERIFIER. ' +
      'Sketch: A fixture whose settlement, `connection_created`, and resolution manifest precede the window\'s `from`, with chunks inside it, appraises verified and its bundle carries all three.',
  );
});
