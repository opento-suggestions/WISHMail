/**
 * T-P5-1 — P-5 (Registry-plural).
 *
 * Classes: CORRESPONDENT.
 * Register: NAMED (§1.4)
 *
 * §A's sketch, verbatim — the scope of this test, which is not widened without
 * a decision (`conformance/README.md`):
 *
 *   The CORRESPONDENT suite passes with each supported resolution profile enabled as the sole profile.
 *
 * NOT EXPANDED. The body below fails on purpose. A test that is not written
 * must not report that it passed, and must say what it is for while it waits.
 */
import { test } from 'node:test';

test('T-P5-1 — Registry-plural', () => {
  throw new Error(
    'T-P5-1 NOT EXPANDED — serves P-5 (Registry-plural), classes CORRESPONDENT. ' +
      'Sketch: The CORRESPONDENT suite passes with each supported resolution profile enabled as the sole profile.',
  );
});
