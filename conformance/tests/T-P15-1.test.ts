/**
 * T-P15-1 — P-15 (Category honesty).
 *
 * Classes: all.
 * Register: NAMED (§1.5)
 *
 * §A's sketch, verbatim — the scope of this test, which is not widened without
 * a decision (`conformance/README.md`):
 *
 *   The conformance claim validates against `spec/schemas/conformance-claim.schema.json`; every test it names exists in the suite.
 *
 * NOT EXPANDED. The body below fails on purpose. A test that is not written
 * must not report that it passed, and must say what it is for while it waits.
 */
import { test } from 'node:test';

test('T-P15-1 — Category honesty', () => {
  throw new Error(
    'T-P15-1 NOT EXPANDED — serves P-15 (Category honesty), classes all. ' +
      'Sketch: The conformance claim validates against `spec/schemas/conformance-claim.schema.json`; every test it names exists in the suite.',
  );
});
