/**
 * T-P5-3 — P-5 (Registry-plural).
 *
 * Classes: POSTMASTER.
 * Register: NAMED (§3.5)
 *
 * §A's sketch, verbatim — the scope of this test, which is not widened without
 * a decision (`conformance/README.md`):
 *
 *   Envelopes resolved through each supported profile are carried identically.
 *
 * NOT EXPANDED. The body below fails on purpose. A test that is not written
 * must not report that it passed, and must say what it is for while it waits.
 */
import { test } from 'node:test';

test('T-P5-3 — Registry-plural', () => {
  throw new Error(
    'T-P5-3 NOT EXPANDED — serves P-5 (Registry-plural), classes POSTMASTER. ' +
      'Sketch: Envelopes resolved through each supported profile are carried identically.',
  );
});
