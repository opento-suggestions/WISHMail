/**
 * T-P1-5 — P-1 (Binding).
 *
 * Classes: CORRESPONDENT, RECIPIENT.
 * Register: NAMED (§7.3)
 *
 * §A's sketch, verbatim — the scope of this test, which is not widened without
 * a decision (`conformance/README.md`):
 *
 *   `spec/vectors/seal.json`: HPKE vectors; reference seals / independent opens, and the reverse.
 *
 * NOT EXPANDED. The body below fails on purpose. A test that is not written
 * must not report that it passed, and must say what it is for while it waits.
 */
import { test } from 'node:test';

test('T-P1-5 — Binding', () => {
  throw new Error(
    'T-P1-5 NOT EXPANDED — serves P-1 (Binding), classes CORRESPONDENT, RECIPIENT. ' +
      'Sketch: `spec/vectors/seal.json`: HPKE vectors; reference seals / independent opens, and the reverse.',
  );
});
