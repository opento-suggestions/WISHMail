/**
 * T-P11-2 — P-11 (Uniform postage).
 *
 * Classes: POSTMASTER.
 * Register: NAMED (§4.5)
 *
 * §A's sketch, verbatim — the scope of this test, which is not widened without
 * a decision (`conformance/README.md`):
 *
 *   `buy_stamp` prices identically across three profiles, three addresses, two buyers; postage for a given weight is profile-independent.
 *
 * NOT EXPANDED. The body below fails on purpose. A test that is not written
 * must not report that it passed, and must say what it is for while it waits.
 */
import { test } from 'node:test';

test('T-P11-2 — Uniform postage', () => {
  throw new Error(
    'T-P11-2 NOT EXPANDED — serves P-11 (Uniform postage), classes POSTMASTER. ' +
      'Sketch: `buy_stamp` prices identically across three profiles, three addresses, two buyers; postage for a given weight is profile-independent.',
  );
});
