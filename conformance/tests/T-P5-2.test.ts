/**
 * T-P5-2 — P-5 (Registry-plural).
 *
 * Classes: RECIPIENT.
 * Register: NAMED (§1.4)
 *
 * §A's sketch, verbatim — the scope of this test, which is not widened without
 * a decision (`conformance/README.md`):
 *
 *   The RECIPIENT suite passes with each supported profile as the sole declared profile.
 *
 * NOT EXPANDED. The body below fails on purpose. A test that is not written
 * must not report that it passed, and must say what it is for while it waits.
 */
import { test } from 'node:test';

test('T-P5-2 — Registry-plural', () => {
  throw new Error(
    'T-P5-2 NOT EXPANDED — serves P-5 (Registry-plural), classes RECIPIENT. ' +
      'Sketch: The RECIPIENT suite passes with each supported profile as the sole declared profile.',
  );
});
