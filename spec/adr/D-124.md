# D-124 — §19 approved; the browser surface stated as unsurveyed

Status:      accepted
Date:        2026-09-07
Decided by:  Sonic
Drafted by:  Claude (ledger §B), Claude Code (this file, 2026-09-07)
Sections:    §19.4
Tests:       none registered in the sections this decision shaped
Supersedes:  approves D-123
Superseded:  none
Spec marker: none — folded into the frozen 0.5.0 text (D-131)

## Context

Two forks were open on §19, one of them about a dependency the specification had never surveyed.

## Decision

The record, from ledger §B row D-124, verbatim:

> **§19 approved** (Sonic 09-07, "Confirmed your MINE. Approved."). Both forks closed: the browser surface — Sonic: "It wasn't unpinned by design, simply no recon was run … the spec should reflect this but not depend on WebMCP pins or recon" — §19.4 now names WebMCP, says the document pins none of it and has surveyed none of it, and leaves pinning to the release that ships the page; D-30 still held for a later version — §19.3's non-blocking `send` paragraph stands. G.1 and G.2 confirmed.

## Alternatives considered

- None recorded. The ledger §B row states the decision without a fork; where a fork was closed, the row says so.

## Consequences

§19 approved, both forks closed. On the browser surface — Sonic: 'It wasn't unpinned by design, simply no recon was run … the spec should reflect this but not depend on WebMCP pins or recon' — §19.4 states plainly that WebMCP is unpinned **and unsurveyed**, that the page is one client of the MCP server, that no requirement depends on it, and that whether to pin it is a question for the release that ships the page. D-30 held: `send` blocks.

## Provenance

Ledger `spec/CONFORMANCE_TESTS_v0_5.md` §B row D-124, dated 2026-09-07 — **the source of truth for this decision**, quoted above without alteration. §18.2 of the specification supplies the title and the sections. Written to a file 2026-09-07 during the ADR completion, so that §18.2's "kept in `spec/adr/`, one file each" is true of every decision and not only of D-1 – D-41. The Context, Alternatives and Consequences sections above are Claude Code's reading of the record and the specification text; where they and ledger §B differ, §B governs, and where either and the specification differ, the specification governs.
