# D-123 — §19 drafted

Status:      accepted
Date:        2026-09-07
Decided by:  Sonic
Drafted by:  Claude (ledger §B), Claude Code (this file, 2026-09-07)
Sections:    §19
Tests:       none registered in the sections this decision shaped
Supersedes:  none
Superseded:  approved by D-124
Spec marker: none — folded into the frozen 0.5.0 text (D-131)

## Context

A frozen specification with unstated open questions invites a reader to design against something that was never decided.

## Decision

The record, from ledger §B row D-123, verbatim:

> **§19 drafted** (MINE in form and grouping; content is sections D and E). Five subsections, informative: 19.2 open at deployment (token pin, mainnet, the `hbar` rate source); 19.3 open in the design (Q-11 classes; Q-7 governance incl. delegated vending; the §16 extensions, other ledgers one tag at a time with CLPR opening only once two tags exist; the §16.8 holds; a non-blocking `send` — D-30 held, stated as undecided for a later version); 19.4 open upstream (NANDA email `nativeId` at HCS-14; Q-10 slot; Draft-standard drift; the browser-side surface — WebMCP unnamed and unpinned because no requirement depends on it, **MINE, needs Sonic's yes**); 19.5 not open (broadcast, broker/key/credit, Solidity, off-consensus scores, Postmaster attestation, private hands). Closed items stay out of §19 — they are history and live in D/E here.

## Alternatives considered

- None recorded. The ledger §B row states the decision without a fork; where a fork was closed, the row says so.

## Consequences

§19 drafted, informative, in five parts: open at deployment (the token pin, mainnet, the `hbar` rate source); open in the design (Q-11 classes, Q-7 governance including delegated vending, the §16 extensions, what §16.8 holds, a non-blocking `send`); open upstream; and **§19.5, not open** — the settled questions the document declines to reopen.

## Provenance

Ledger `spec/CONFORMANCE_TESTS_v0_5.md` §B row D-123, dated 2026-09-07 — **the source of truth for this decision**, quoted above without alteration. §18.2 of the specification supplies the title and the sections. Written to a file 2026-09-07 during the ADR completion, so that §18.2's "kept in `spec/adr/`, one file each" is true of every decision and not only of D-1 – D-41. The Context, Alternatives and Consequences sections above are Claude Code's reading of the record and the specification text; where they and ledger §B differ, §B governs, and where either and the specification differ, the specification governs.
