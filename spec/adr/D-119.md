# D-119 — §18 drafted

Status:      accepted
Date:        2026-09-06
Decided by:  Sonic
Drafted by:  Claude (ledger §B), Claude Code (this file, 2026-09-07)
Sections:    §18
Tests:       none registered in the sections this decision shaped
Supersedes:  none
Superseded:  approved by D-121
Spec marker: none — folded into the frozen 0.5.0 text (D-131)

## Context

The decisions, the identifier concordance, the pins and the repository layout were spread across the ledger with no pointer from the specification.

## Decision

The record, from ledger §B row D-119, verbatim:

> **§18 appendices drafted** (MINE in form; content is this ledger's). §18.2 ADR index = section B as a title table, D-42 – D-117, each with the sections it shaped, pointing at `spec/adr/`; D-1 – D-41 pointed to, not repeated. §18.3 concordance = C.2, identifiers only, marked informative (Sonic named it for §18; it sits in tension with resume item 6 — G.5 fork). §18.4 pins = a pointer to §1.6 and the `spec/pins.json` shape. §18.5 repository layout from the project brief (RECORD: spec/ conformance/ app/) with the fourteen schema file names (MINE). No requirement in §18.

## Alternatives considered

- None recorded. The ledger §B row states the decision without a fork; where a fork was closed, the row says so.

## Consequences

§18 drafted: §18.2 as an ADR index giving each decision its title and the sections it shaped, pointing at `spec/adr/`; §18.3 the identifier concordance; §18.4 the pins appendix naming `spec/pins.json`; §18.5 the repository layout and the fourteen schema names. Informative; it indexes the record and binds nothing. **This ADR set is what §18.2 points at**, and writing it is what these files are.

## Provenance

Ledger `spec/CONFORMANCE_TESTS_v0_5.md` §B row D-119, dated 2026-09-06 — **the source of truth for this decision**, quoted above without alteration. §18.2 of the specification supplies the title and the sections. Written to a file 2026-09-07 during the ADR completion, so that §18.2's "kept in `spec/adr/`, one file each" is true of every decision and not only of D-1 – D-41. The Context, Alternatives and Consequences sections above are Claude Code's reading of the record and the specification text; where they and ledger §B differ, §B governs, and where either and the specification differ, the specification governs.
