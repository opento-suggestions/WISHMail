# D-92 — Pins filled from the canonical repository

Status:      accepted
Date:        2026-09-06
Decided by:  Sonic
Drafted by:  Claude (ledger §B), Claude Code (this file, 2026-09-07)
Sections:    §1.6
Tests:       T-P9-1, T-P9-2 — the tests registered in the sections this decision shaped (derived from their `Conformance:` notes, not from this decision adding them)
Supersedes:  none
Superseded:  none
Spec marker: none — folded into the frozen 0.5.0 text (D-131)

## Context

Four of six standards are Draft and change by pull request, and 'conforms to HCS-10' means nothing without a content identifier for the text.

## Decision

The record, from ledger §B row D-92, verbatim:

> **Pins filled** (§1.6) from the Claude Code recon of 2026-09-06: canonical repo `hiero-ledger/hiero-consensus-specifications` @ `7046156c85eaaf29e149fa10232964d33a58d34e`; per-file git blobs in §1.6, SHA-256s in `spec/pins.json` (= `recon/pins.draft.json`). `hashgraph-online/hcs-improvement-proposals` no longer holds the text (gitignored, synced from hiero at build time since 2026-01-15); its README's "canonical" line is stale. Pin form is `{repo, commit, path, blobSha, sha256}`.

## Alternatives considered

- None recorded. The ledger §B row states the decision without a fork; where a fork was closed, the row says so.

## Consequences

Pins filled from the Claude Code recon of 2026-09-06: the canonical repository is `hiero-ledger/hiero-consensus-specifications` at commit `7046156c`, with per-file git blobs in §1.6 and SHA-256s in `spec/pins.json`. `hashgraph-online/hcs-improvement-proposals` no longer holds the text — gitignored and synced from hiero at build time since 2026-01-15 — and its README's 'canonical' line is stale. Pin form `{repo, commit, path, blobSha, sha256}`; digests over raw blob bytes.

## Provenance

Ledger `spec/CONFORMANCE_TESTS_v0_5.md` §B row D-92, dated 2026-09-06 — **the source of truth for this decision**, quoted above without alteration. §18.2 of the specification supplies the title and the sections. Written to a file 2026-09-07 during the ADR completion, so that §18.2's "kept in `spec/adr/`, one file each" is true of every decision and not only of D-1 – D-41. The Context, Alternatives and Consequences sections above are Claude Code's reading of the record and the specification text; where they and ledger §B differ, §B governs, and where either and the specification differ, the specification governs.
