# D-44 — The pedestal: one stamp token; the Postmaster holds its supply

Status:      accepted
Date:        2026-09-05
Decided by:  Sonic
Drafted by:  Claude (ledger §B), Claude Code (this file, 2026-09-07)
Sections:    §3.5, §4.1
Tests:       T-P2-1, T-P2-2, T-P4-2, T-P5-3, T-P9-2, T-P11-1, T-P13-1 — the tests registered in the sections this decision shaped (derived from their `Conformance:` notes, not from this decision adding them)
Supersedes:  none
Superseded:  the token named $POSTAGE by D-100
Spec marker: none — folded into the frozen 0.5.0 text (D-131)

## Context

HIP-991 collects a custom fee in one token, and P-11 requires one price for every buyer. Read together they force the stamp to be a single token, and its issuer to be whoever holds that token's supply key.

**Citation added 2026-09-07 (D-135).** The HIP-991 half of that reading stood on inference until §1.6 pinned the text. It holds: `hip-991.md:88` — "A custom fee can be set in HBAR or HTS fungible tokens and must have an accountID as the receiver" — one denominating token and one collector account per fee, with at most ten fees on a topic (`:133`). `hashgraph/hedera-improvement-proposal` @ `03701720`, blob `179b61be…`. FETCHED from here on.

## Decision

The record, from ledger §B row D-44, verbatim:

> The pedestal: the stamp is one token and the Postmaster is whoever holds its supply key. Forced by HIP-991 single-token fees + P-11. Delegated vending is a Q-7 question.

## Alternatives considered

- Several stamp tokens, one per deployment or per registry — rejected: a HIP-991 fee names one token, and P-11 would then have to hold across tokens that trade against each other.

## Consequences

The Postmaster is defined by what it holds rather than by who it is (§3.5): there is one stamp, and the Postmaster is whoever issues it. Who that party should be, and whether a second may vend from the same supply, is Q-7 and stays open (§19.3).

## Provenance

Ledger `spec/CONFORMANCE_TESTS_v0_5.md` §B row D-44, dated 2026-09-05 — **the source of truth for this decision**, quoted above without alteration. §18.2 of the specification supplies the title and the sections. Written to a file 2026-09-07 during the ADR completion, so that §18.2's "kept in `spec/adr/`, one file each" is true of every decision and not only of D-1 – D-41. The Context, Alternatives and Consequences sections above are Claude Code's reading of the record and the specification text; where they and ledger §B differ, §B governs, and where either and the specification differ, the specification governs.
