# D-98 — nanda: keying, address form, v2 only

Status:      accepted
Date:        2026-09-06
Decided by:  Sonic
Drafted by:  Claude (ledger §B), Claude Code (this file, 2026-09-07)
Sections:    §9.4
Tests:       T-P6-2 — the tests registered in the sections this decision shaped (derived from their `Conformance:` notes, not from this decision adding them)
Supersedes:  completes D-97
Superseded:  none
Spec marker: none — folded into the frozen 0.5.0 text (D-131)

## Context

With the profile re-founded, its address form, its keying, and which index generation it speaks were open.

## Decision

The record, from ledger §B row D-98, verbatim:

> §9.4 closures (Sonic 09-06): org/agent keying as drafted (`org.wishmail` / `org.wishmail.agents[slug]`, no fallback); the `https://` document address dropped; the profile speaks the **v2** index exclusively (§9.4 heading and address text say so); HCS-14 `nativeId` for `urn:ai:email:` identities held (G.4).

## Alternatives considered

- None recorded. The ledger §B row states the decision without a fork; where a fork was closed, the row says so.

## Consequences

Org and agent keying as drafted — `org.wishmail` and `org.wishmail.agents[slug]`, with no fallback between them; the `https://` document address dropped; the profile speaks the **v2** index exclusively. Nothing in the read path is signed, nothing is content-addressed, and `ttl_seconds` is stored and echoed but never enforced — so every `nanda` resolution is endorsed `blurred`, and `stale` is a discipline of this document (§9.4, L-8).

## Provenance

Ledger `spec/CONFORMANCE_TESTS_v0_5.md` §B row D-98, dated 2026-09-06 — **the source of truth for this decision**, quoted above without alteration. §18.2 of the specification supplies the title and the sections. Written to a file 2026-09-07 during the ADR completion, so that §18.2's "kept in `spec/adr/`, one file each" is true of every decision and not only of D-1 – D-41. The Context, Alternatives and Consequences sections above are Claude Code's reading of the record and the specification text; where they and ledger §B differ, §B governs, and where either and the specification differ, the specification governs.
