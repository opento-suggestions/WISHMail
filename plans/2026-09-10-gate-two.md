# Sequence 4, A′, and Gate Two — the letter

**2026-09-10.** Written at the close of 2026-09-09, after Gate One ran, and carrying the rulings of that evening so
that this window opens without the conversation that produced them. HEAD at the start is the close-of-day commit,
tree clean, tag `v0.5.10`.

**Everything below §1 is RECORD (Sonic, 2026-09-09) unless marked otherwise.** Where this plan disagrees with
`spec/WISHMAIL_SPEC_v0_5.md`, the specification wins; where it disagrees with what was built, `app/OPERATIONS.md` and
the git history are the record of what actually happened.

---

## 0. Where yesterday left it

**Correspondent B is provisioned on `hedera:testnet` and resolves under `hcs14` and `hol` with no `blurred`.**

```
account 0.0.10452127   doorbell 0.0.10452149   log 0.0.10452150   manifest 0.0.10452154
declaration registry 0.0.10452155   profile file 0.0.10452158   registration 0.0.6913983#381
```

Its eight mailbox rows were signed by the agent and paid for by the Postmaster — §6.1's carry, run for the first time
against the network — and its registration was paid by its own account, which is what §9.5 reads to decide `blurred`.
Its `StampReceipt` is the first this counter has issued.

**Correspondent A stopped after its transfer and stays exactly as it is.** Account `0.0.10451893`, 12 `$POSTAGE`,
0.05 ℏ, reference `0.0.8641261@1789006030.569861064` restored to its home from consensus alone, no mailbox and **no
receipt**. It is the counter's first sale that stopped, and it is recorded as that. `ENTITIES.md` marks it `stopped`;
LIMITATIONS says why the receipt is permanently absent and why it was not reconstructed. **Do not finish A.** Its home
is kept.

**Eight defects were found and fixed**, every one in the window between a signature leaving the buyer and the buyer
learning the outcome — the window no offline check reaches, because there is no offline consensus node. They are not
proven fixed by any check. **A′ is what proves them.**

**One number was measured that changes a decision**: the doorbell costs **26.31542199 ℏ**, and one mailbox costs the
Postmaster **27.78102934 ℏ** against **15.09094832 ℏ** taken for the sale.

---

## 1. Sequence 4 — the price, before anything else

**A new `PriceList` on `0.0.10426551`, sequence 4, identical to sequence 3 except `provisioning.unitPrice`.**
`registrationFee` is unchanged at `"0.05"`. No schema moves and no wire string moves: §14.3 makes the schedule the
sequence of messages, so this supersedes by landing after and neither earlier message is edited.

**OPEN — the number is Sonic's.** 30 ℏ was the lean, against 27.78 ℏ measured. **Prepared and validated today; signed
on Sonic's word and not before.**

Reuse `publishPriceList(options)` in `app/src/ops/pricelist2.ts` — it was generalised for exactly this, and the stop
that makes it idempotent is that the topic must hold exactly N−1 messages now. Add `app/src/ops/pricelist4.ts` calling
it with `{ suffix: '-4', sequence: 4, key: 'prices.fourth', role: …, why: … }`, and
`app/price-list-4.hedera-testnet.json` beside its three siblings. Validate against the **registered** `PriceList`
schema before signing; byte-compare on readback; confirm sequences 1, 2 and 3 read back untouched.

`ENTITIES.md`'s price table gains a fourth row and its "the last one governs" line still holds.

---

## 2. Wallet top-ups — funding, not sales

**`0.0.10450879` and `0.0.10450880` topped up from the Postmaster's payer to ~100 ℏ each.** A purchase at the new price
with twelve stamps is ~43 ℏ, and the ring and the letter cost more on top of that.

**Recorded in `app/OPERATIONS.md` as funding and not a sale**, in the same terms and the same section as the original
demo-operator funding: nothing touches the price list, the treasury, `$POSTAGE`, or the counter; no quote is issued, no
reference settles, and no `StampReceipt` exists, because nothing is being sold. Read each balance back from the mirror
under a named predicate.

---

## 3. A′ — the end-to-end proof of the eight fixes

**A fresh home under C1OPERATOR's wallet `0.0.10450879`.** An operator may own many agents: the wallet is the
operator's, the home is the agent's, and a fresh home is a new agent (D-165). A′ is not A repaired and must not be
described as one.

Provisioned through the fixed counter at sequence 4's price, in D-159's order, with every stop condition live:

```
   boot            keys born once, in the agent's own process
   buy_stamp       provision: true — the three-legged transfer, the eight carried rows, the receipt
   register_agent  the agent as its own payer, 0.02 ℏ declared
   resolve         self, under hcs14 AND hol, with no `blurred` on either
```

**This run is the proof of the eight fixes and nothing else is.** Expect it to complete in one pass; if it stops,
report what is true at the stop and what is resumable, and wait — the resume path is the thing being tested as much as
the happy path.

Then `npm run entities:md -- --homes <parent>`, `check:entities` green, and the run of record under Step 5 with every
transaction id and mirror readback.

---

## 4. Gate Two — the letter

**After A′, and not before.** Its gate report is already written as **Step 6** in `app/OPERATIONS.md`; **amend it for
A′ and sequence 4 first** — its §1 still describes a "fixture" whose account is funded rather than bought, and says so
itself. The rule every step since the probe has followed holds: **the report is written and committed before the first
signature.**

```
   A′ --send--> B          one certified letter, with returnReceipt
   B  --inbox-->           opens it; the AAD verifies or it does not open
   B  --ack-->             the return receipt, on B's manifest topic
   stranger --verify-->    from a third, EMPTY home: no key, no account, no stamp (P-4)
   B  --send--> A′         a reply on the SAME lane, ringing nothing (§7.1)
```

What Gate Two owes on the way in: `send`, `inbox` and `ack` are built and exercised end to end against the modelled
ledger (`check:letter`, 63 assertions) and **refuse on the Correspondent's MCP naming the gate**. What they need is the
live wiring to `Consensus`, §10.4's schedule — which `send` still refuses outright — and a letter to carry.

The second letter is the one that proves §7.1: the earliest-created open lane between two agents is *the* lane, the
doorbell watcher decides from consensus, and a second letter to the same recipient rings nothing.

---

## 5. The demo, and what it does not show live

**Provisioning happens before recording, not on video.** The demo shows provisioning as **facts already on
consensus** — the receipt card scrolled up in goose, HashScan links to the topics — and spends its minutes on the
letter loop and the stranger's verify, which are the two claims WISHMail sells. Friday is goose by hand.

This is a recording decision and not a scoping one: everything shown as a fact was signed, and `ENTITIES.md` is what a
judge follows to check it.

---

## 6. What is not in this window

The `dns` and `nanda` resolvers · the CLI · the WebMCP send side · the `x402-usdc` purchase leg · HCS-25 · rotation ·
Postmaster-pays carry for an agent's own submissions outside a purchase, so **T-P4-2 stays untested** · expanding any
of the eighty-six conformance tests, and therefore any conformance claim. **LIMITATIONS names our scoping as the reason
for each — never Hedera, never the specification.**

---

## Verification

1. The battery: seventeen `check:*` plus `npm run typecheck` and `npm run p13:check`, green before and after.
2. `npm run check:register` — 86 both ways, 0 expanded.
3. Sequence 4 validated against the registered schema, byte-compared on readback, sequences 1–3 untouched.
4. A′ completes in one pass and resolves under both profiles with **no `blurred`**; a second run creates nothing and
   exits 0.
5. `npm run check:entities` green with A′ in DEMO AGENTS and A still marked `stopped`.
6. The letter: opened by `inbox`, acknowledged, reconciled by `verify` from an empty home, and the reply ringing
   nothing.
7. Tree clean, every commit pushed with its tags, HEAD reported.

## Report at the end

What was signed, with every transaction id and mirror readback; what the battery prints; the commit the tree is clean
at; and anything that diverged on the way — brought rather than coded around.
